import { StyleSheet } from "react-native";
import Color from "./Color";

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Color.default_backgroundColor
  }
})